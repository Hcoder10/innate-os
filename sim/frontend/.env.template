# The frontend no longer reads build-time VITE_* env vars. Endpoint URLs are now
# loaded at runtime from /config.json:
#   - dev (`yarn dev`):     edit public/config.json
#   - container (Docker):   set the env vars consumed by docker-entrypoint.sh
#                           (SIM_BASE_URL, WS_BASE_URL, ROBOT_WS_URL, DIRECT_ROBOT,
#                            CARTESIA_API_KEY) — see ../docker-compose.dev.yml
