# CI test image bakes in ros2_ws source, config, and workspace at build
# time (no bind mounts in CI). Allow what Dockerfile.test COPYs from.
**

!ros2_ws/
!ros2_ws/**
!config/
!config/**
!workspace/
!workspace/**
!ci/
!ci/run_integration_tests.sh
